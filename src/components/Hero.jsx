import React from 'react';
import HeroCarousel from './HeroCarousel';

const Hero = () => {
  return (
    <section id="home" className="relative">
      <HeroCarousel />
    </section>
  );
};

export default Hero;
